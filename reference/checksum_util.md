# generalized util function for sequence multiplication

Util function for ISBN, ean, SSN providers. for cases such as
`sum(x[4]*5 + x[3]*4 + x[2]*3 + x[1] *2)`

## Usage

``` r
checksum_util(vector, multiplicationvector)
```
